{
    "configurations": [
        {
            "command": "npm run start:debug",
            "name": "run server",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}/server"
        }
    ]
}
